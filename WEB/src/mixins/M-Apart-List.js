// import api from "../plugins/axios/Req-Module";

export default {
  methods: {
    getApartList() {
      // api("get", "/aparts").then((i) => {
      //   console.log(i);
      // });
    },

    // mApartList() {
    //   axios({ method: "post", url: "/login", data: { username, pwd } })
    //     .then(async (i) => {
    //       localStorage.setItem("token", i.data.token);
    //       await this.$router.push("/dashboard");
    //       return;
    //     })
    //     .catch((e) => {
    //       console.log(e.response);
    //       return;
    //     });
    // },
  },
};
